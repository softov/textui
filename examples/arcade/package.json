{
  "name": "@textui/example-arcade",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "A cabinet of games: snake, tetris and breakout on a canvas",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@textui/core": "workspace:*",
    "@textui/terminal": "workspace:*",
    "@textui/widgets": "workspace:*"
  },
  "devDependencies": {
    "@textui/testing": "workspace:*"
  }
}
