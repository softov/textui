{
  "name": "@textui/example-chat",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "An agent chat client: sessions, a streaming transcript, and answering what the agent asks",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@microsoft/agent-host-protocol": "^0.8.0",
    "@textui/core": "workspace:*",
    "@textui/terminal": "workspace:*"
  },
  "devDependencies": {
    "@textui/testing": "workspace:*"
  }
}
