{
  "name": "@textui/example-showcase",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Everything on one screen, as wrapping rows of panels - the example that exists to be screenshotted",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@textui/core": "workspace:*",
    "@textui/terminal": "workspace:*",
    "@textui/widgets": "workspace:*"
  },
  "devDependencies": {
    "@textui/testing": "workspace:*"
  }
}
